parameters:
  timeoutInMinutes: 5
steps:
  - checkout: tests
    displayName: TESTS clone
    path: $(System.DefaultWorkingDirectory)/_tests
    timeoutInMinutes: ${{ parameters.timeoutInMinutes }}
    condition: and(succeeded(), not(eq(variables.tests_clone, false)))